package Algorithms.Graph.Utils;

import java.util.Comparator;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.stream.Collectors;

public class Tools {
    /**
     * one vs one HashMap's switching keys and values
     */
    public static<T extends Objects> HashMap<T,T> mapSwitch(HashMap<T,T> map){
        HashMap<T,T> res = new HashMap<>();
    }
}
